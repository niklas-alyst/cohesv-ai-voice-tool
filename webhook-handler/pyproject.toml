[project]
name = "webhook-handler"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "boto3>=1.40.52",
    "twilio>=9.8.4",
]

[dependency-groups]
dev = [
    "dotenv>=0.9.9",
    "httpx>=0.28.1",
    "pytest>=8.4.2",
]
