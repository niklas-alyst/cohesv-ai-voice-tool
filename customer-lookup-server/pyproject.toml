[project]
name = "customer-lookup-server"
version = "0.1.0"
description = "Customer lookup Lambda function for ai-voice-tool"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "boto3>=1.40.66",
    "pytest>=8.4.2",
]
